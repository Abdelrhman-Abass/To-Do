from django.db import models

# Create your models here.
# class Task(models.Model):
#     task_name = models.CharField( max_length= 300, null=False,blank=False) 
#     status = models.CharField(max_length=60, default='in progress')

